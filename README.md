We first added a new user to the users table - Shawn, Harrell, 44. 
We then changed the users first name from 'Shawn' to 'Hero' using the username.
Next we deleted username 'jdoe' and userids 6 and 12. 
We then queried for the first name and age of the username sharrell. 
Next we queried for the first 5 rows after row 2. 
Finally we put the users in descending order by age, and returned the first 7 rows after row 5. This query reminded us about the 
orders of operations because the list was first ordered, then the first 7 rows were pulled which returned the row 5.  9